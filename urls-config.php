<?php
  // Database Address
  // [ '%ip address%' / '%domain%' ]
  // PHP >= 7.0.0 not work localhost. use 127.0.0.1.
  define('dbhost',        '127.0.0.1');

  // Database username
  // [ '%username%' ]
  define('dbuser',        null);

  // Database password
  // [ '%password%' ]
  define('dbpasswd',      null);

  // Database name
  // [ '%database name%' ]
  define('dbname',        null);

  // Database table prefix
  // [ '%table prefix%' ]
  define('dbprefix',      'urls_');

  // Database charset
  // [ '%charset%' ]
  define('dbcharset',     'utf8mb4');

  // Admin user name
  // [ '%username%' ]
  define('adminuser',     'admin');

  // Admin user password
  // [ '%password%' ]
  define('adminpasswd',   'alpine');

  // Admin user mail
  // [ '%e-mail address%' ]
  define('adminemail',    'admin@mail.example.com');

  // Register mail
  // [ '%e-mail address%' ]
  define('registemail',   'admin@mail.example.com');

  // Infomation mail
  // [ '%e-mail address%' ]
  define('infoemail',     'info@mail.example.com');

  // Server scheme
  // [ 'http' / 'https' / null(responsive) ]
  define('scheme',        null);

  // Domain name
  // [ '%your domain%' / null(responsive) ]
  define('urlsdomain',    null);

  // Anonymous adduser
  // [ true / false ]
  define('adduser',       true);
  
